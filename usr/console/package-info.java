/**
 * This package provides classes that deal with processing
 * network connections and requests to a ManagementConsole of a
 * component of the system.
 * It handles each command of the protocol.
 */
package usr.console;